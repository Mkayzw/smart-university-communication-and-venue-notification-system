{
  "name": "smart-university-vercel-deployment",
  "version": "1.0.0",
  "description": "Smart University Communication and Venue Notification System - Vercel Deployment Configuration",
  "scripts": {
    "vercel-build": "cd BACKEND && npm install && npx prisma generate",
    "build": "cd BACKEND && npm install && npx prisma generate",
    "test-local": "cd BACKEND && npm run start-services",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "bcryptjs": "^3.0.3",
    "jsonwebtoken": "^9.0.2",
    "axios": "^1.13.2",
    "expo-server-sdk": "^4.0.0",
    "express-rate-limit": "^7.1.5",
    "http-proxy-middleware": "^2.0.9",
    "swagger-ui-express": "^5.0.0",
    "socket.io": "^4.6.1"
  },
  "devDependencies": {
    "prisma": "^5.22.0",
    "vercel": "^28.0.0",
    "nodemon": "^3.0.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}